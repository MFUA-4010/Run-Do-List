import 'package:flutter/material.dart';

class RunDoList extends StatelessWidget {
  const RunDoList({super.key});

  @override
  Widget build(final BuildContext context) {
    return MaterialApp(
      theme: ThemeData.light(
        useMaterial3: true,
      ),
    );
  }
}
